mutation AddRide($input: AddRideInput!) {
  addRide(input: $input) {
    id
    startTime
    durationSeconds
    distanceMeters
    elevationGainMeters
    rideType
    bikeId
    location
    notes
  }
}
