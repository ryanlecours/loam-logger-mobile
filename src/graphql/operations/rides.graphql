query RecentRides($take: Int) {
  rides(take: $take) {
    id
    startTime
    durationSeconds
    distanceMeters
    elevationGainMeters
    rideType
    bikeId
    location
    weather {
      id
      tempC
      condition
    }
  }
}
