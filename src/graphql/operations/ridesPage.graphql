query RidesPage($take: Int, $after: ID, $filter: RidesFilterInput) {
  rides(take: $take, after: $after, filter: $filter) {
    id
    startTime
    durationSeconds
    distanceMeters
    elevationGainMeters
    averageHr
    rideType
    bikeId
    unownedBike
    location
    notes
    garminActivityId
    garminDeviceName
    stravaActivityId
    stravaDeviceName
    whoopWorkoutId
    suuntoWorkoutId
    weather {
      id
      tempC
      feelsLikeC
      precipitationMm
      windSpeedKph
      humidity
      wmoCode
      condition
    }
  }
}
